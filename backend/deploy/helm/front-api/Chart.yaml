apiVersion: v2
name: front-api
description: Auth (JWT access+refresh in httpOnly cookies), admin user CRUD, and a caching proxy layer to analytics_api for the future React dashboard
type: application
version: 0.1.0
appVersion: "1.0.0"
