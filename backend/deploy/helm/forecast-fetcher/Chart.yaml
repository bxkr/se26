apiVersion: v2
name: forecast-fetcher
description: Consumes weather.need_info (dataset_type=forecast), resolves station coordinates via regions-api, fetches forecast-as-of-the-day weather from Open-Meteo, writes raw JSON to S3, publishes weather.forecast.raw.created
type: application
version: 0.1.0
appVersion: "1.0.0"
